export class BudgetItem {
    // description: string;
    // amount: number;

    constructor(public description: string, public amount: number) {
        // no need to write two lines below if we made description and amount public
        // good bless typescript

        // this.description = description;
        // this.amount = amount;
    }
}